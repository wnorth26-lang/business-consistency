# Security Policy

Do not report vulnerabilities publicly. Enable private vulnerability reporting before production use.

Read-only scopes should be used by default. Never commit API keys, tokens, database passwords, customer data, or `.env` files.
